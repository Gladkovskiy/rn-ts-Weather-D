import {ISearchScreen} from '../types'

const translation: ISearchScreen = {
  screenTitle: 'The choice of the city',
  weatherByLocation: 'Weather by location',
  placeholderSearchInput: 'City name',
  locationError: 'Turn on location',
  noPermission: 'No permission to determine the location',
}

export default translation
